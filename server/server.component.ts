import { Component } from '@angular/core';

@Component({
    selector: 'app-server', //created an HTML selector which is usable in .html files 
    templateUrl: './server.component.html' //tells the html that should be run for this typescript file
})

export class ServerComponent {

}